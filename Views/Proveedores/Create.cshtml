@model SourcingManager.Models.Proveedor

@{
    ViewData["Title"] = "Nuevo proveedor";
}

<div class="page-header">
    <div>
        <span class="page-kicker">Alta</span>
        <h1 class="page-title">Nuevo proveedor</h1>
        <p class="page-subtitle">Completa los datos fiscales y de contacto para registrar un proveedor.</p>
    </div>
</div>

<div class="page-panel form-panel">
    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="form-group">
            <label asp-for="Nombre" class="form-label"></label>
            <input asp-for="Nombre" class="form-control" placeholder="Nombre comercial" />
            <span asp-validation-for="Nombre" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="RucONit" class="form-label">RUC / NIT</label>
            <input asp-for="RucONit" class="form-control" placeholder="Documento fiscal" />
            <span asp-validation-for="RucONit" class="text-danger"></span>
        </div>
        <div class="row">
            <div class="col-md-6 form-group">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control" placeholder="correo@empresa.com" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="col-md-6 form-group">
                <label asp-for="Telefono" class="form-label"></label>
                <input asp-for="Telefono" class="form-control" placeholder="Teléfono" />
                <span asp-validation-for="Telefono" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="Direccion" class="form-label"></label>
            <input asp-for="Direccion" class="form-control" placeholder="Dirección" />
            <span asp-validation-for="Direccion" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="CategoriaProveedorId" class="form-label">Categoría</label>
            <select asp-for="CategoriaProveedorId" class="form-select" asp-items="ViewBag.CategoriaProveedorId"></select>
            <span asp-validation-for="CategoriaProveedorId" class="text-danger"></span>
        </div>

        <div class="form-actions">
            <input type="submit" value="Guardar proveedor" class="btn btn-primary" />
            <a asp-action="Index" class="btn btn-outline-secondary">Volver al listado</a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
